# M0 checkpoint

- Date: 2026-08-21
- Milestone: Repo and quality gates
- Status: Complete
- Scope: Repository shell only; M1 product UI has not started

Verified locally:

- Prettier formatting check
- TypeScript strict typecheck
- ESLint
- Vitest smoke test
- Next.js production build

Playwright is configured with a smoke test. Running it locally requires the Playwright Chromium
binary; CI or a developer machine can install it with `pnpm exec playwright install chromium`.

The permanent checkpoint archive excludes dependencies, build output, local environment files,
test output and unrelated working files.
