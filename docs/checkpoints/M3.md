# M3 — Hosted Supabase foundation

Status: database complete; email OTP client implemented; hosted email template pending activation.

## Hosted project

- Organization: Mentiq
- Project: Mattis (`ccpyhexgpiqdmtwvzjdd`)
- Region: Stockholm (`eu-north-1`)
- Confirmed project cost: USD 0/month
- Database status at verification: `ACTIVE_HEALTHY`

## Verification evidence

- 11 public tables, all with RLS enabled
- 37 public-table RLS policies
- 4 owner-scoped policies on the private homework Storage bucket
- Authenticated CRUD grant verified on `public.sessions`
- Anonymous `SELECT` denied on `public.sessions`
- Private homework bucket verified as non-public
- Security advisor: zero findings
- Performance advisor: no missing foreign-key indexes; only expected unused-index information on
  the empty database
- Hosted migration history mirrored under `supabase/migrations/`
- TypeScript database types generated to `apps/web/lib/database.types.ts`

## Remaining activation work

- Install `supabase/templates/magic-link.html` in the hosted Auth email templates.
- Configure custom SMTP if the default trial provider does not deliver to the invited plus-address.
- Replace the minimal server-side Auth/Data HTTP adapter with the official Supabase JavaScript
  client when package-registry access is available.
- Configure project URL and publishable key in local/deployment environment variables.
- Run cross-user Auth, RLS, and Storage tests with two synthetic email users.
- Implement retention cleanup before any real-student pilot.

## Repository handoff

The source tree is prepared for the GitHub repository `JacoboHansen/Mattis`. No credentials or
student data belong in the repository.
